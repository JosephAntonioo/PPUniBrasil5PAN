server:
  port: 8080

spring:
  application:
    name: crawler
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enable: false
  datasource:
    url: jdbc:mysql://localhost:3306/microservice_schema?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: 123Abc
